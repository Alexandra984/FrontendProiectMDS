import React from "react";

const InfoItem = props => {
    return (
        <div>
            <h1>{props.title}</h1>
            <p>{props.gendre}</p>
            <p>{props.link}</p>
            <p>{props.artist}</p>
            bauuuu
        </div>
    )
}

export default InfoItem;